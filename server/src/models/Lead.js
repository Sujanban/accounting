const mongoose = require("mongoose"); const { applyAuditFields } = require("./schemaHelpers");
const leadSchema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, branchId: { type: mongoose.Schema.Types.ObjectId, ref: "Branch", required: true }, name: { type: String, trim: true, required: true, maxlength: 200 }, phone: { type: String, trim: true, maxlength: 30, default: null }, email: { type: String, trim: true, lowercase: true, maxlength: 254, default: null }, source: { type: String, trim: true, maxlength: 100, default: null }, stage: { type: String, enum: ["NEW", "QUALIFIED", "PROPOSAL", "WON", "LOST"], default: "NEW" }, notes: { type: String, trim: true, maxlength: 2000, default: null } }, { timestamps: true }); leadSchema.index({ companyId: 1, branchId: 1, stage: 1, createdAt: -1 }); applyAuditFields(leadSchema); module.exports = { Lead: mongoose.model("Lead", leadSchema) };
