const mongoose = require("mongoose"); const { applyAuditFields } = require("./schemaHelpers"); const projectTaskSchema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, projectId: { type: mongoose.Schema.Types.ObjectId, ref: "Project", required: true }, title: { type: String, trim: true, required: true, maxlength: 200 }, dueDate: { type: Date, default: null }, status: { type: String, enum: ["TODO", "IN_PROGRESS", "DONE"], default: "TODO" } }, { timestamps: true }); projectTaskSchema.index({ companyId: 1, projectId: 1, status: 1 }); applyAuditFields(projectTaskSchema); module.exports = { ProjectTask: mongoose.model("ProjectTask", projectTaskSchema) };
