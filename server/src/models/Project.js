const mongoose = require("mongoose"); const { applyAuditFields } = require("./schemaHelpers"); const projectSchema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, branchId: { type: mongoose.Schema.Types.ObjectId, ref: "Branch", required: true }, name: { type: String, trim: true, required: true, maxlength: 200 }, code: { type: String, trim: true, uppercase: true, required: true }, customerId: { type: mongoose.Schema.Types.ObjectId, ref: "Contact", default: null }, startDate: { type: Date, default: null }, endDate: { type: Date, default: null }, budget: { type: Number, min: 0, default: 0 }, status: { type: String, enum: ["PLANNED", "ACTIVE", "COMPLETED", "ON_HOLD"], default: "PLANNED" } }, { timestamps: true }); projectSchema.index({ companyId: 1, code: 1 }, { unique: true }); applyAuditFields(projectSchema); module.exports = { Project: mongoose.model("Project", projectSchema) };
