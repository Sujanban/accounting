const mongoose = require("mongoose"); const { applyAuditFields } = require("./schemaHelpers"); const leaveRequestSchema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, employeeId: { type: mongoose.Schema.Types.ObjectId, ref: "Employee", required: true }, leaveType: { type: String, enum: ["ANNUAL", "SICK", "UNPAID", "OTHER"], required: true }, startDate: { type: Date, required: true }, endDate: { type: Date, required: true }, reason: { type: String, trim: true, maxlength: 1000, default: null }, status: { type: String, enum: ["PENDING", "APPROVED", "REJECTED"], default: "PENDING" } }, { timestamps: true }); leaveRequestSchema.index({ companyId: 1, employeeId: 1, startDate: -1 }); applyAuditFields(leaveRequestSchema); module.exports = { LeaveRequest: mongoose.model("LeaveRequest", leaveRequestSchema) };
