const mongoose = require("mongoose"); const { applyAuditFields } = require("./schemaHelpers");
const fixedAssetSchema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, branchId: { type: mongoose.Schema.Types.ObjectId, ref: "Branch", required: true }, warehouseId: { type: mongoose.Schema.Types.ObjectId, ref: "Warehouse", default: null }, category: { type: String, required: true, trim: true, maxlength: 100 }, purchaseDate: { type: Date, required: true }, purchaseValue: { type: Number, required: true, min: 0 }, salvageValue: { type: Number, required: true, min: 0, default: 0 }, usefulLifeMonths: { type: Number, required: true, min: 1 }, depreciationMethod: { type: String, enum: ["STRAIGHT_LINE", "WRITTEN_DOWN_VALUE"], required: true }, status: { type: String, enum: ["ACTIVE", "DISPOSED"], default: "ACTIVE" }, disposalTransactionId: { type: mongoose.Schema.Types.ObjectId, ref: "Transaction", default: null }, disposedAt: { type: Date, default: null } }, { timestamps: true }); fixedAssetSchema.index({ companyId: 1, branchId: 1, status: 1 }); applyAuditFields(fixedAssetSchema); module.exports = { FixedAsset: mongoose.model("FixedAsset", fixedAssetSchema) };
