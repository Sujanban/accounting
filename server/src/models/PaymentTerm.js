const mongoose = require("mongoose"); const { applySoftDeleteFields, applyAuditFields } = require("./schemaHelpers");
const schema = new mongoose.Schema({ companyId: { type: mongoose.Schema.Types.ObjectId, ref: "Company", required: true }, name: { type: String, required: true, trim: true }, dueDays: { type: Number, required: true, min: 0, max: 3650 }, description: { type: String, trim: true, default: null }, isActive: { type: Boolean, default: true } }, { timestamps: true }); applySoftDeleteFields(schema); applyAuditFields(schema); schema.index({ companyId: 1, name: 1 }, { unique: true }); module.exports = { PaymentTerm: mongoose.model("PaymentTerm", schema) };
