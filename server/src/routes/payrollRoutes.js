const express = require("express"); const { requireAuth, resolveActiveCompany, resolveActiveFiscalYear, requireRoles } = require("../middleware/auth"); const { requireCompletedOnboarding } = require("../middleware/onboarding"); const { Employee } = require("../models/Employee"); const { Branch } = require("../models/Branch"); const { asyncHandler } = require("../utils/asyncHandler"); const { sendSuccess } = require("../utils/apiResponse"); const { ApiError } = require("../utils/apiError"); const payrollRouter = express.Router(); payrollRouter.use(requireAuth, resolveActiveCompany, resolveActiveFiscalYear, requireCompletedOnboarding); payrollRouter.get("/employees", requireRoles("OWNER", "ADMIN", "ACCOUNTANT", "STAFF"), asyncHandler(async (req, res) => sendSuccess(res, 200, "Employees fetched successfully.", await Employee.find({ companyId: req.auth.activeCompanyId, isActive: true }).sort({ employeeCode: 1 }).lean()))); payrollRouter.post("/employees", requireRoles("OWNER", "ADMIN"), asyncHandler(async (req, res) => { const { branchId, employeeCode, name, baseSalary, email } = req.body; if (!branchId || !employeeCode || !name || !Number.isFinite(baseSalary) || baseSalary < 0) throw new ApiError(422, "Branch, employee code, name, and base salary are required."); const branch = await Branch.findOne({ _id: branchId, companyId: req.auth.activeCompanyId, isActive: true }).lean(); if (!branch) throw new ApiError(422, "The selected branch is unavailable."); const employee = await Employee.create({ companyId: req.auth.activeCompanyId, branchId, employeeCode: employeeCode.trim().toUpperCase(), name: name.trim(), baseSalary, email: email?.trim().toLowerCase() || null, createdBy: req.auth.user._id, updatedBy: req.auth.user._id }); return sendSuccess(res, 201, "Employee created successfully.", employee); })); module.exports = { payrollRouter };
