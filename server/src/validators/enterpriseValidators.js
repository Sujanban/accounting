function validateBranch(body) { const errors = []; const allowed = new Set(["branchCode", "name", "address", "phone", "email", "managerId", "isDefault"]); for (const key of Object.keys(body)) if (!allowed.has(key)) errors.push({ field: key, message: "This field cannot be modified." }); if (!body.branchCode || !/^[A-Za-z0-9_-]{2,20}$/.test(body.branchCode.trim())) errors.push({ field: "branchCode", message: "Branch code must be 2-20 letters, numbers, underscores, or hyphens." }); if (!body.name || body.name.trim().length < 2) errors.push({ field: "name", message: "Branch name is required." }); if (body.isDefault !== undefined && typeof body.isDefault !== "boolean") errors.push({ field: "isDefault", message: "Default flag must be true or false." }); return errors; }
function validateWarehouse(body) { const errors = []; const allowed = new Set(["branchId", "warehouseCode", "name", "address", "description", "isDefault"]); for (const key of Object.keys(body)) if (!allowed.has(key)) errors.push({ field: key, message: "This field cannot be modified." }); if (!body.branchId) errors.push({ field: "branchId", message: "Branch is required." }); if (!body.warehouseCode || !/^[A-Za-z0-9_-]{2,20}$/.test(body.warehouseCode.trim())) errors.push({ field: "warehouseCode", message: "Warehouse code must be 2-20 letters, numbers, underscores, or hyphens." }); if (!body.name || body.name.trim().length < 2) errors.push({ field: "name", message: "Warehouse name is required." }); if (body.isDefault !== undefined && typeof body.isDefault !== "boolean") errors.push({ field: "isDefault", message: "Default flag must be true or false." }); return errors; }
module.exports = { validateBranch, validateWarehouse };
