import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import { Card, Flex, Heading, Text } from "@radix-ui/themes"; import { useState } from "react"; import { Button } from "../../components/ui/button"; import { AppSelect } from "../../components/ui/select"; import { useBranches } from "../enterprise/use-enterprise"; import { fixedAssetsApi } from "./fixed-assets-api";
export function FixedAssetsPage() { const client = useQueryClient(); const assets = useQuery({ queryKey: ["fixed-assets"], queryFn: ({ signal }) => fixedAssetsApi.list(signal) }); const create = useMutation({ mutationFn: fixedAssetsApi.create, onSuccess: () => client.invalidateQueries({ queryKey: ["fixed-assets"] }) }); const schedule = useMutation({ mutationFn: fixedAssetsApi.schedule }); const branches = useBranches(); const [branchId, setBranchId] = useState(""); return <Flex direction="column" gap="5"><div><Heading size="7">Fixed assets</Heading><Text color="gray">Asset register and read-only depreciation preview. No accounting entries are created here.</Text></div><Card size="3"><form className="accounting-form" onSubmit={(e) => { e.preventDefault(); const f = new FormData(e.currentTarget); create.mutate({ branchId, assetCode: String(f.get("assetCode")), category: String(f.get("category")), purchaseDate: String(f.get("purchaseDate")), purchaseValue: Number(f.get("purchaseValue")), salvageValue: Number(f.get("salvageValue")), usefulLifeMonths: Number(f.get("usefulLifeMonths")), depreciationMethod: String(f.get("depreciationMethod")) as "STRAIGHT_LINE" | "WRITTEN_DOWN_VALUE" }); }}><label>Branch<AppSelect value={branchId} onChange={(e) => setBranchId(e.target.value)} required><option value="">Select branch</option>{branches.data?.map((b) => <option key={b.id} value={b.id}>{b.name}</option>)}</AppSelect></label><label>Asset code<input name="assetCode" required /></label><label>Category<input name="category" required /></label><label>Purchase date<input name="purchaseDate" type="date" required /></label><label>Purchase value<input name="purchaseValue" type="number" min="0" required /></label><label>Salvage value<input name="salvageValue" type="number" min="0" defaultValue="0" required /></label><label>Useful life (months)<input name="usefulLifeMonths" type="number" min="1" required /></label><label>Method<AppSelect name="depreciationMethod" defaultValue="STRAIGHT_LINE"><option value="STRAIGHT_LINE">Straight line</option><option value="WRITTEN_DOWN_VALUE">Written down value</option></AppSelect></label><div className="accounting-form__actions"><Button type="submit" disabled={!branchId} loading={create.isPending}>Add asset</Button></div></form></Card><Card size="3"><Heading size="4">Asset register</Heading>{assets.data?.map((asset) => <Flex key={asset.id} justify="between" mt="2"><Text>{asset.assetCode} · {asset.category} · {asset.status}</Text><Button size="1" variant="outline" onClick={() => schedule.mutate(asset.id)}>Preview depreciation</Button></Flex>) || <Text color="gray">No fixed assets yet.</Text>}{schedule.data ? <table className="accounting-table"><thead><tr><th>Month</th><th>Depreciation</th><th>Closing value</th></tr></thead><tbody>{schedule.data.items.map((item) => <tr key={item.month}><td>{item.month}</td><td>{item.depreciation.toFixed(2)}</td><td>{item.closingValue.toFixed(2)}</td></tr>)}</tbody></table> : null}</Card></Flex>; }
