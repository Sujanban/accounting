import { Card, Flex, Heading, Text } from "@radix-ui/themes"; import { useNavigate } from "react-router-dom"; import { Button } from "../../components/ui/button";
export function PosPage() { const navigate = useNavigate(); return <Flex direction="column" gap="5"><div><Heading size="7">Point of sale</Heading><Text color="gray">Create counter sales through the existing Sales Voucher workflow.</Text></div><Card size="3"><Heading size="4">New counter sale</Heading><Text as="p" color="gray" mt="2">POS uses the transaction engine to validate stock, tax, and balanced accounting entries before posting a sale.</Text><Button mt="4" onClick={() => navigate("/vouchers/sales/new")}>Start POS sale</Button></Card><Card size="3"><Heading size="4">Scope</Heading><Text color="gray">Offline sync, barcode scanning, receipt printing, and payment-terminal integrations remain out of scope for this initial POS surface.</Text></Card></Flex>; }
